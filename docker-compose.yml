services:
  devpalette:
    build: .
    ports:
      - "3210:80"
    volumes:
      - ../palette_data:/data
    environment:
      - OLLAMA_HOST=boss-mac.mermaid-ghost.ts.net
      - OLLAMA_PORT=11434
      - OLLAMA_MODEL=smollm2:latest
      - MAX_PALETTES=20
    restart: unless-stopped
